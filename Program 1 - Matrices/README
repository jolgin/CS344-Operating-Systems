This program is a bash shell script that calculates basic matrix operations using input from either a file or stdin. The input will be whole number values separated by tabs int0 a rectangular matrix. 

The operations supported are calculating matrix dimensions, transposition, matrix mean, adding matrices, and multiplying matrices.

This program was developed using SublimeText and tested on Mac's terminal program.